import React from "react";
import { Container } from "./styles";

export const TwoButtonContainer = ({children}) => {
    return (
        <Container>
            {children}
        </Container>
    )
}